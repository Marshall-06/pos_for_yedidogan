# ── Server ──
PORT=4000
NODE_ENV=development
# Frontend domeni (CORS) — birnäçe bolsa wergul bilen aýyr
CORS_ORIGIN=*

# ── PostgreSQL ──
DB_HOST=localhost
DB_PORT=5433
DB_NAME=yedidogan
DB_USER=postgres
DB_PASSWORD=gizlin
# Sequelize loglary konsola çykarsynmy
DB_LOGGING=false

# ── JWT ──
JWT_SECRET=change_this_to_a_long_random_secret
JWT_EXPIRES_IN=7d

# Bellik: taýýar admin döredilmeýär. Ilkinji /api/auth/register bilen
# özüň admin döredýärsiň (boş bazada ilkinji ulanyjy awtomatiki admin bolýar).
